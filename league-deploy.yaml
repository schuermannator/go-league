apiVersion: apps/v1
kind: Deployment
metadata:
  name: league
  labels:
    app: league
spec:
  replicas: 1
  selector:
    matchLabels:
      app: league
  template:
    metadata:
      labels:
        app: league
    spec:
      containers:
      - name: league
        image: docker.io/schuermannator/league
        ports:
        - containerPort: 8080

---

kind: Service
apiVersion: v1
metadata:
  name: league
spec:
  selector:
    app: league
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
